import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  snackbar: {
    zIndex: 999,
    position: 'absolute',
    top: 0,
    left: 0,
    right: 0,
    margin: 16,
    borderRadius: 4,
    elevation: 10,
  },
});
