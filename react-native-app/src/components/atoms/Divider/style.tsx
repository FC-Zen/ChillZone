import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  divider: {
    height: 1,
    backgroundColor: '#E0E0E0', // Couleur grise pour la ligne
    marginVertical: 4, // Espacement léger autour du divider
  },
});
